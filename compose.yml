services:
  website:
    build:
      context: ./Frontend
    image: ${IMAGE_NAME_FRONTEND}
    container_name: ${CONTAINER_NAME_FRONTEND}
    restart: unless-stopped
    ports:
      - "${APP_PORT}:80"
    env_file: .env
